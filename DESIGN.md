# 工时追踪器 - 前端功能清单

## 底部导航栏

| Tab | 图标 | 说明 |
|-----|------|------|
| 首页 | 🏠 | 默认首页 |
| 日历 | 📅 | 日历视图 + 热力图 |
| 设置 | ⚙️ | 配置与数据管理 |

---

## 首页（Dashboard）

### 顶部
- 标题：「考勤补给站」
- 当前日期：YYYY年M月D日 星期X

### 打卡区（居中）
- 大圆形按钮（180×180，绿色）
  - 默认态：「下班打卡」+「点击记录下班时间」
  - 已打卡态：「已打卡」+「点击撤销」（灰色）
- 打卡后显示：下班时间 HH:mm · 工时 Xh
- 请假按钮：圆形灰色按钮（44×44），标「休」字
  - 激活态变深灰

### 本周进度卡
- 标题：本周进度
- 数值：当前工时h / 目标工时h
- 进度条（绿色=达标，橙色=未达标）
- 统计：缺口 Xh、请假 X天（如有）

### 本月进度卡
- 标题：本月进度
- 数值：当前工时h / 目标工时h
- 进度条
- 统计：缺口 Xh、请假 X天、需加班 X天（如有）

### 底部提示（当有缺口时）
- 橙色背景卡片：「本月工时缺口 Xh，折合需加班 X 天」

---

## 日历页（Calendar）

### 月份导航
- 居中显示：YYYY年M月
- 左右箭头切换月份

### 日历网格
- 表头：一 二 三 四 五 六 日
- 每天格子显示：
  - 日期数字
  - 今日有绿色圆环标识
  - 未来日期灰色不可点
  - 有记录时底部显示工时（颜色标识状态）
    - 绿色 = 达标
    - 橙色 = 未达标
    - 灰色 = 请假（显示「休」）

### 点击日期弹窗（底部弹出）
- 标题：YYYY-MM-DD
- 关闭按钮（✕）
- 如果已请假：
  - 提示「当日已标记为请假」
  - 「取消请假」按钮
- 如果未请假：
  - 输入框：下班时间（HH:mm）
  - 「保存」按钮
  - 「标记请假」按钮
- 底部信息：当日工时 Xh

### 年度热力图
- 月份标签：1月 - 12月
- 热力图方块（类似 GitHub 贡献图）
  - 颜色梯度：无数据 → 浅绿 → 中绿 → 深绿 → 最深绿
- 图例：少 → 色块 → 多

### 每月出勤统计
- 标题：每月出勤
- 12 个格子，每格显示：月份 + 出勤天数

---

## 设置页（Settings）

### 工时配置区（只读展示）

| 配置项 | 默认值 |
|--------|--------|
| 上班时间 | 09:00 |
| 午休时长 | 1.0 小时 |
| 每日目标工时 | 10.5 小时 |
| 加班基准时间 | 22:30 |

### 月度配置区（可编辑）

| 配置项 | 交互方式 |
|--------|----------|
| 本月应出勤天数 | 输入框 + 保存按钮（范围 1-31） |
| 当前周类型 | 点击切换：大周 ↔ 小周 |

### 数据管理区
- 「导出数据」按钮 → 导出 JSON 文件
- 「导入数据」按钮 → 从备份文件恢复

---

## 颜色系统

| 用途 | 色值 | 说明 |
|------|------|------|
| 背景 | #F5F5F7 | 浅灰底 |
| 卡片 | #FFFFFF | 白色 |
| 达标/确认 | #34C759 | 绿色 |
| 缺口/警示 | #FF9F0A | 橙色 |
| 主文本 | #1C1C1E | 深黑 |
| 次文本 | #8E8E93 | 灰色 |
| 分割线 | #E5E5EA | 浅灰 |
| 请假 | #AEAEB2 | 中灰 |
| 休息日 | #D1D1D6 | 浅灰 |

---

## 数据模型（供参考）

### 工时配置（AppConfig）
| 字段 | 类型 | 说明 |
|------|------|------|
| dailyStartTime | string | 上班时间，如 "09:00" |
| lunchBreakHours | number | 午休扣除小时数 |
| targetDailyHours | number | 每日目标工时 |
| overtimeEndTime | string | 加班基准下班时间 |
| monthlyTargetDays | number | 本月应出勤天数 |
| currentWeekType | enum | 大周 / 小周 |

### 每日考勤记录（WorkRecord）
| 字段 | 类型 | 说明 |
|------|------|------|
| dateId | string | 日期 ID，格式 YYYY-MM-DD |
| clockOutTime | string \| null | 下班打卡时间，null=未打卡 |
| actualWorkHours | number | 当日净工时（小时） |
| isLeave | boolean | 是否全天请假 |

### 工时统计（WorkStats）
| 字段 | 类型 | 说明 |
|------|------|------|
| totalHours | number | 实际工时总和 |
| targetHours | number | 目标工时总和 |
| gap | number | 工时缺口（正=不足，负=溢出） |
| leaveDays | number | 请假天数 |
| overtimeDaysNeeded | number | 需加班天数 |
